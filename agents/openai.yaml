interface:
  display_name: "Pareto Startup Thinking"
  short_description: "Find the bottleneck, the vital few moves, and what to kill"
  default_prompt: "Use $pareto-startup-thinking to choose the few startup bets worth doing now, name what to defer or kill, and give the smallest high-leverage next step."

policy:
  allow_implicit_invocation: false
